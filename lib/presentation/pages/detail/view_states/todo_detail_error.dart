import 'package:flutter/material.dart';

class TodoDetailError extends StatelessWidget {
  const TodoDetailError({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Text('ERROR on DetailPage, please try again'),
    );
  }
}
